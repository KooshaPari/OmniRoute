//! omniroute-compression: RTK + Caveman compression (scaffold).

#![deny(unsafe_code)]
#![warn(missing_docs)]

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert!(true);
    }
}
