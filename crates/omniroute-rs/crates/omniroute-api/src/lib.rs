//! omniroute-api: axum HTTP API (scaffold).

#![deny(unsafe_code)]
#![warn(missing_docs)]

// Module tree lands in follow-up PRs after absorb/omniroute-rs.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert!(true);
    }
}
