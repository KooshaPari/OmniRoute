name: l45-p99-regression
permissions:
  contents: read
on:
  schedule: [{cron: '0 3 * * 1'}]
  workflow_dispatch: {}
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - run: echo "P99 latency regression detection — compare current vs baseline"
