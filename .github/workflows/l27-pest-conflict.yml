name: l27-pest-conflict
on:
  schedule: [{cron: '0 7 * * 1'}]
  workflow_dispatch: {}
jobs:
  scan:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - run: echo "PEST conflict scanner — feature flag interaction matrix"

