name: l21-bom-diff
permissions:
  contents: read
on:
  schedule: [{cron: '0 5 * * 1'}]
  workflow_dispatch: {}
jobs:
  diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - run: echo "BOM diff — detect Cargo.lock drift vs baseline"
