name: l52-sbom-audit
on:
  schedule: [{cron: '0 9 * * 1'}]
  workflow_dispatch: {}
jobs:
  audit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - run: echo "SBOM license audit — review new dependency licenses"

