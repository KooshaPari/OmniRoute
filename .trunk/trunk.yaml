# Trunk.io Code Quality configuration.
# The v0.1 schema is required by current trunk-action releases.
# https://docs.trunk.io/check/reference

version: 0.1

cli:
  version: 1.25.0

plugins:
  sources:
    - id: trunk
      ref: v1.9.0
      uri: https://github.com/trunk-io/plugins

lint:
  disabled:
    - eslint
  enabled:
    - prettier@3.9.4
  ignore:
    - linters: [ALL]
      paths:
        - docs/sessions/20260719-cross-chat-alignment/HANDOFFS.md
