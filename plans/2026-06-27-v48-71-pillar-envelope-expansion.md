# v48 — 71-Pillar Envelope Expansion

**Date:** 2026-06-27
**Status:** PLANNED
**Previous wave:** v47 (cycle 35 automation — T1..T4 shipped)
**Fleet mean:** 3.72 (sustained across 12 cycles)

## Scope

Expand the governance envelope to 20 unboarded repos per `dag-state/wave-1.json`. Each repo receives its missing fleet-baseline files (AGENTS.md, justfile, SSOT.md, llms.txt, deny.toml, .pre-commit-config.yaml, .github/workflows/ci.yml) as determined by a per-repo inventory scan.

## Tracks

| Track | Artifact | Repos affected | Expected delta |
|-------|----------|----------------|----------------|
| T1 | deny.toml | Parpoura, PhenoHandbook, PhenoMCPServers, clap-ext, heliosBench, mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, pheno-cdylib-bridge, phenoResearchEngine, phenodag, phenodag-tool, phenodocs | 13× files |
| T2 | justfile | TestingKit, clap-ext, heliosBench, mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, pheno-cdylib-bridge, pheno-tracing, phenoEvents, phenodag, phenodag-tool, phenodocs | 12× files |
| T3 | SSOT.md | Sidekick, TestingKit, clap-ext, eyetracker, heliosBench, kmobile, mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, pheno-cdylib-bridge, pheno-tracing, phenoData, phenoEvents, phenoResearchEngine, phenodag, phenodag-tool, phenodocs | 17× files |
| T4 | llms.txt | Sidekick, clap-ext, eyetracker, heliosBench, kmobile, mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, phenoData, phenoEvents, phenoResearchEngine, phenodag-tool, phenodocs | 13× files |
| T5 | .pre-commit-config.yaml | TestingKit, clap-ext, kmobile, mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, pheno-cdylib-bridge, pheno-tracing, phenoEvents, phenodag | 10× files |
| T6 | .github/workflows/ci.yml | eyetracker, mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, pheno-cdylib-bridge, phenodag, phenodag-tool, phenodocs | 8× files |
| T7 | AGENTS.md | mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp, phenodag, phenodag-tool | 5× files |
| T8 | v48 closure + cycle-36 probe + v49 plan | monorepo | 3× artifacts |

**Total:** 81 new files across 20 repos.

## Dependencies

- All tracks are independent (no cross-repo ordering constraint).
- Each repo's files can be authored and PR'd in parallel.
- Per-repo PR: one commit per target repo, all landing on `chore/v48-envelope-expansion-2026-06-27` branches.

## Risk

- **clap-ext** needs special handling: it's a nested repo (per AGENTS.md L5-155 pattern). Files go into `clap-ext/` and must respect its own `.gitignore` and index. Use `git -C clap-ext` for direct worktree operations.
- **mobile-cli, mobile-cli-mobile-mcp-wt, mobile-mcp** are the most gapped (7 files each); they'll need the most attention per repo.
- **pheno-tracing, pheno-cdylib-bridge** are Rust crates with existing CI (proven); adding justfile + SSOT.md + .pre-commit-config.yaml should be low risk.

## Close criteria

- [ ] All 81 files authored, committed, and pushed across 20 repos
- [ ] 0 CI regressions across any onboarded repo
- [ ] Cycle-36 probe written confirming fleet mean ≥3.72
- [ ] v49 plan drafted with next priorities

---

Generated by `tools/pillar-fleet/cliff-sync.sh` / manual probe.
