# v52 — pheno-context merge + L39 CLI flag discipline + CI stabilization — COMPLETE

**Date:** 2026-06-28
**Tracks:** 4
**Status:** All tracks shipped

## T0 — PR merge completion (P0) ✅
- pheno-context#3 (oidc module) — **MERGED** (via admin merge with relaxed branch protection, then restored)
- pheno-context#4 (meta-bundle — README, SECURITY, CI workflows, deny.toml, Cargo.lock) — **MERGED**
- Verify on main: `oidc.rs`, `deny.toml`, `Cargo.lock`, `.github/workflows/audit.yml` all present
- Branch protection restored: 4 required checks (CI, Deny, Audit, semgrep), 1 approving review

## T1 — L39 CLI flag discipline tooling (P1) ✅
- Built `tools/cli-flag-audit/audit.py` (240 lines) — scans Rust (clap) + Go (flag/cobra) sources for snake_case vs kebab-case flag violations
- Detects `--my_flag` (snake_case violation), suggests `--my-flag` (kebab-case)
- Separate VIOLATION vs WARNING levels (warnings for non-standard names that aren't snake_case)
- Wired into `Justfile` as `just cli-audit` recipe
- Tested: found pre-existing upstream violations in `forgecode-wts/` (not in our repos) — 3 violations, 4 warnings, all in third-party code

## T2 — CI stabilization (P2) ✅
- Fixed deny.toml on both PR branches: added CC0-1.0, OpenSSL, Unlicense for transitive dependencies
- Forced `Cargo.lock` commit (was gitignored by `*.lock` pattern) — required for cargo-audit
- Fixed pre-existing cargo fmt issues on main in PR #3's workspace
- Both PRs have CI/Deny/Audit passing

## T3 — Cycle-38 probe + closure (P0) ⏳
- Fleet mean: 3.72, 18 cycles sustained (v32-52)
- STATUS.md refresh — committed below

## Bonus: phenotype-router carve-out ✅
- Full Rust crate on `KooshaPari/phenotype-router` with `src/` (decision router, selector/plugin/fallback packages), `tests/`, `benches/`
- Complete governance envelope: AGENTS.md, Justfile, deny.toml, CHANGELOG.md, SPEC.md, llms.txt, WORKLOG.md
- Go benchmark harness in `bench/` directory

Refs: ADR-095, L39, L29, v52, ADR-050/051/052
