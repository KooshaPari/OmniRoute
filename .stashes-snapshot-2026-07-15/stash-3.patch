[1mdiff --git a/design.md b/design.md[m
[1mindex 64794e44a..e71f88c77 100644[m
[1m--- a/design.md[m
[1m+++ b/design.md[m
[36m@@ -1,6 +1,6 @@[m
 # OmniRoute — Design System & Visual Identity[m
 [m
[31m-> **Status:** standardization plan. **Phases 1–3 are implemented in this PR** (grid, primitives, status-color centralization, mono token, and the DataTable token migration). The DataTable migration is **faithful** — dark stays byte-identical (the new `--table-*` dark values equal the old hardcoded rgba); light is fixed (it was buggy always-dark via dead `var()` fallbacks). **⚠️ Wants a visual pass before merge** (light-theme tables + the secondary-text shift `#888`→`--color-text-muted`). **Phase 4 is now largely done too** (C6 focus-ring → accent, C7 Checkbox/Textarea primitives, C9 `cn()`→tailwind-merge); only the selective C8 hex-sweep remains. Note several remaining "hardcoded" hex are _intentional_ (always-dark console terminal, ReactFlow SVG strokes) and must NOT be swept. **Phase 5 (D4 + D8): the grid now reaches every standalone screen** (login/auth/error/legal/status/onboarding — their opaque `bg-bg` full-screen wrappers were hiding it) **and the dashboard content shell is fluid up to 4K** (`max-w-7xl` → `max-w-[3840px]`) so it follows the viewport on large monitors instead of centering with wide side gutters. **Phase 6 (D9): data tables are now opaque surfaces** so the grid no longer bleeds through their rows — card-less tables paint `bg-surface`, and the two log tables' semi-transparent `bg-black/5` tint (which tailwind-merge let win over the Card's `bg-surface`) is removed. The grid size itself is already correct (32px, identical to the site); a "bigger" grid on a running instance is a stale build, not code.[m
[32m+[m[32m> **Status:** analysis + standardization plan (no code applied yet — this doc is the spec to approve before implementation).[m
 > **Date:** 2026-06-16 · **Scope:** unify the OmniRoute dashboard (`src/`) with the marketing site (`_mono_repo/omnirouteSite/`) into **one visual identity** — same graph-paper grid background, same color tokens, standardized components.[m
 [m
 ---[m
[36m@@ -13,7 +13,7 @@[m [m## 1. Purpose[m
 2. A handful of **shared design tokens** the site has but the dashboard lacks (radius scale, brand gradient, `surface-2`, mono font).[m
 3. **Component-level consistency** — a number of dashboard components bypass the theme tokens with hardcoded hex/rgba.[m
 [m
[31m-This document is the analysis and the plan.[m
[32m+[m[32mThis document is the analysis and the plan. **Nothing is changed until approved.**[m
 [m
 ---[m
 [m
[36m@@ -22,7 +22,7 @@[m [m## 2. Principles[m
 - **Single source of truth = `src/app/globals.css`.** The site mirrors the dashboard, never the other way around. New tokens land in `globals.css` first.[m
 - **Tokens, never literals.** Components consume semantic tokens (`bg-surface`, `text-primary`, `border-border`), never raw `#hex`.[m
 - **Subtle, not loud.** The grid is a faint wallpaper that sits behind content — it must never reduce text contrast or fight the UI.[m
[31m-- **Theme-aware.** Everything works in both `.dark` (the product's signature look) and light.[m
[32m+[m[32m- **Theme-aware.** Everything works in both `.dark` (default-ish, the product's signature look) and light.[m
 - **Surgical rollout.** Ship the grid + tokens first (low risk, high visibility), then component cleanups in waves.[m
 [m
 ---[m
[36m@@ -49,25 +49,24 @@[m [m### 3.1 Colors — already unified ✅[m
 [m
 ### 3.2 Gaps — what the dashboard is missing[m
 [m
[31m-| Gap                     | Site has                                                                       | Dashboard                                                | Action                 |[m
[31m-| ----------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------------- | ---------------------- |[m
[31m-| **Grid wallpaper**      | `body::before` graph-paper, `--grid-line`, `--grid-size 32px`, `--section-alt` | **✅ added (Phase 1)**                                   | **Part A**             |[m
[31m-| **Radius scale**        | `--radius 14px`, `--radius-sm 9px`                                             | `--radius 14px` added; `-sm` + component repoint pending | **Part B / Phase 2**   |[m
[31m-| **Brand gradient**      | `--grad-brand 135deg primary→accent-3`                                         | **✅ token added (Phase 1)**; consumed in Phase 2        | **Part B**             |[m
[31m-| **Nested surface**      | `--surface-2 #1c2230`                                                          | **✅ added (Phase 1)**                                   | **Part B**             |[m
[31m-| **Mono font**           | `--font-mono` (ui-monospace stack)                                             | pending (Phase 4, with consumers)                        | **Part B**             |[m
[31m-| **`text-muted` (dark)** | `#8b8b9e`                                                                      | `#a1a1aa` (zinc-400)                                     | reconcile — **Part B** |[m
[32m+[m[32m| Gap                     | Site has                                                                       | Dashboard                                                  | Action                 |[m
[32m+[m[32m| ----------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------- | ---------------------- |[m
[32m+[m[32m| **Grid wallpaper**      | `body::before` graph-paper, `--grid-line`, `--grid-size 46px`, `--section-alt` | none (flat `--color-bg`)                                   | **Part A**             |[m
[32m+[m[32m| **Radius scale**        | `--radius 14px`, `--radius-sm 9px`                                             | none — primitives use ad-hoc `rounded-md/lg/xl` (6/8/12px) | **Part B**             |[m
[32m+[m[32m| **Brand gradient**      | `--grad-brand 135deg primary→accent-3`                                         | none — only a one-off `.bg-hero-gradient`                  | **Part B**             |[m
[32m+[m[32m| **Nested surface**      | `--surface-2 #1c2230`                                                          | none                                                       | **Part B**             |[m
[32m+[m[32m| **Mono font**           | `--font-mono` (ui-monospace stack)                                             | none (code/terminal areas have no token)                   | **Part B**             |[m
[32m+[m[32m| **`text-muted` (dark)** | `#8b8b9e`                                                                      | `#a1a1aa` (zinc-400)                                       | reconcile — **Part B** |[m
 [m
 ### 3.3 Theming mechanics (so we don't break anything)[m
 [m
 - **Tailwind v4, CSS-first** (no `tailwind.config.*`). Tokens are defined in `:root`/`.dark` and exposed to utilities via `@theme inline` (`globals.css:130-179`).[m
 - **Dark via `.dark` class** on `<html>` (`@custom-variant dark` at `globals.css:22`), toggled by a custom Zustand store (`src/store/themeStore.ts`), default theme = `system` (`src/shared/constants/appConfig.ts:11`). The site uses `html[data-theme="light"]` instead — **the mechanisms differ but never meet** (separate origins), so no conflict. We keep the dashboard's `.dark` mechanism.[m
[31m-- **Runtime primary override** exists (`themeStore.ts:85-97`, presets in `COLOR_THEMES`) — users can swap `--color-primary`. Any new token (gradient, etc.) that references `--color-primary` inherits those overrides for free. ✅[m
[31m-- **Tailwind v4 reserved radius names:** `--radius-sm/md/lg/...` back the `rounded-*` utilities. Redefining them retroactively changes every existing `rounded-*` (e.g. `rounded-sm` is used in 12 files). So the small-radius value and component repoint are deliberately deferred to Phase 2, where consumers change together.[m
[32m+[m[32m- **Runtime primary override** exists (`themeStore.ts:85-97`, presets in `COLOR_THEMES`) — users can swap `--color-primary`. Any new token (gradient, etc.) that references `--color-primary` will inherit those overrides for free. ✅[m
 [m
 ---[m
 [m
[31m-## 4. Part A — The graph-paper grid background (headline ask) — IMPLEMENTED (Phase 1)[m
[32m+[m[32m## 4. Part A — The graph-paper grid background (headline ask)[m
 [m
 ### 4.1 What it is[m
 [m
[36m@@ -91,30 +90,35 @@[m [m### 4.1 What it is[m
 [m
 ### 4.2 Precedent already in the codebase[m
 [m
[31m-`src/app/landing/page.tsx:16-26` **already implements this same grid per-page** — but with **red** lines (`#E54D5E`, opacity `0.06`) at **50px**, plus animated orbs. So the pattern is proven in the product; this work promotes it to a **global, theme-aware** wallpaper.[m
[32m+[m[32m`src/app/landing/page.tsx:16-26` **already implements this same grid per-page** — but with **red** lines (`#E54D5E`, opacity `0.06`) at **50px**, plus animated orbs. So the pattern is proven in the product; we are promoting it to a **global, theme-aware** wallpaper and (optionally) retiring the duplicate.[m
 [m
[31m-### 4.3 Tokens added (in `globals.css`)[m
[32m+[m[32m### 4.3 Tokens to add (in `globals.css`)[m
 [m
 ```css[m
 :root {[m
[31m-  /* light — grid opacity tuned up from the site's 0.045 so the wallpaper is[m
[31m-     actually visible on the dense dashboard (cards/chrome cover most of the viewport) */[m
[31m-  --grid-line: rgba(0, 0, 0, 0.07);[m
[31m-  --grid-size: 32px;[m
[32m+[m[32m  /* light */[m
[32m+[m[32m  --grid-line: rgba(0, 0, 0, 0.045);[m
[32m+[m[32m  --grid-size: 46px;[m
   --section-alt: rgba(0, 0, 0, 0.022);[m
 }[m
 .dark {[m
[31m-  /* dark — tuned up from 0.035 for the same reason */[m
[31m-  --grid-line: rgba(255, 255, 255, 0.06);[m
[32m+[m[32m  /* dark */[m
[32m+[m[32m  --grid-line: rgba(255, 255, 255, 0.035);[m
   --section-alt: rgba(255, 255, 255, 0.018);[m
 }[m
 ```[m
 [m
[31m-### 4.4 The single blocker — removed[m
[32m+[m[32m### 4.4 The single blocker[m
 [m
[31m-The grid is global by construction (it covers the panel, `auth`/`login`, error pages — every route — at once). Exactly **one** element hid it inside the panel:[m
[32m+[m[32mThe grid is global by construction (it covers the panel, `auth`/`login`, error pages — every route — at once). Exactly **one** element hides it inside the panel:[m
 [m
[31m-- `src/shared/components/layouts/DashboardLayout.tsx` — the outer wrapper painted an opaque `bg-bg`. Everything below it is already transparent (`<main>`, the scroll container, the `max-w-7xl` inner), so **removing `bg-bg`** lets the body grid show through the content area (the body's `--color-bg` remains the base fill).[m
[32m+[m[32m- `src/shared/components/layouts/DashboardLayout.tsx:62` — the outer wrapper paints an opaque `bg-bg`:[m
[32m+[m
[32m+[m[32m  ```jsx[m
[32m+[m[32m  <div className="flex h-dvh min-h-0 w-full overflow-hidden bg-bg">[m
[32m+[m[32m  ```[m
[32m+[m
[32m+[m[32m  Everything below it is already transparent — `<main>` (`:93`), the scroll container (`:102`), the `max-w-7xl` inner (`:103`). So **removing `bg-bg` from this one line** lets the body grid show through the entire content area (the body's `--color-bg` remains the base fill underneath the grid).[m
 [m
   ```diff[m
   - <div className="flex h-dvh min-h-0 w-full overflow-hidden bg-bg">[m
[36m@@ -123,99 +127,128 @@[m [m### 4.4 The single blocker — removed[m
 [m
 ### 4.5 Chrome interaction (sidebar / header)[m
 [m
[31m-- `Header` (`Header.tsx:207`, `bg-bg`) and `Sidebar` (`Sidebar.tsx:430`, `bg-sidebar`) stay **opaque** → the grid shows in the **content area only**, with solid chrome framing it. Calm default, matches how the site separates chrome from canvas (decision D3 = solid).[m
[32m+[m[32m- `Header` (`src/shared/components/Header.tsx:207`, `bg-bg`) and `Sidebar` (`src/shared/components/Sidebar.tsx:430`, `bg-sidebar`) stay **opaque** → the grid shows in the **content area only**, with solid chrome framing it. This is the recommended, calm default and matches how the site separates chrome from canvas.[m
[32m+[m[32m- _Optional vibrancy variant:_ make the header translucent (`bg-bg/80 backdrop-blur`) so the grid runs behind it. A `.bg-vibrancy` helper already exists (`globals.css:370`). **Decision D3 below.**[m
 [m
 ### 4.6 Login / auth / error pages[m
 [m
[31m-These render directly under `<body>` (no panel chrome), so the global grid should appear behind them automatically. **Phase 5 — DONE:** the standalone full-screen wrappers were in fact opaque (`min-h-screen … bg-bg`, where `bg-bg` is the same solid fill as `<body>`), which hid the grid on every non-dashboard screen — not just login. All of them are now transparent so the shared wallpaper shows through: `login`, `forgot-password`, `callback`, `maintenance`, `offline`, `status`, `terms`, `privacy`, `onboarding`, and `ErrorPageScaffold` (covers `400`/`401`). This closes **D4** (extended from login-only to every standalone screen). Guarded by `tests/unit/design-grid-background.test.ts`.[m
[32m+[m[32mThese render directly under `<body>` (no panel chrome) and their page wrappers are mostly transparent — the global grid appears behind them automatically. One exception: `src/app/login/page.tsx:124,139` uses opaque `bg-bg` wrappers; soften the same way if we want the grid there too (minor, **D4**).[m
 [m
 ### 4.7 Landing page[m
 [m
[31m-`landing/page.tsx` keeps its richer animated background (orbs + vignette) — its own marketing splash (decision D5 = leave as-is).[m
[32m+[m[32m`landing/page.tsx` keeps its richer animated background (orbs + vignette). Options: (a) leave it as-is (its own splash identity), or (b) align its grid to the global tokens (46px, neutral lines) for consistency. **Recommend (a)** — it's a marketing splash, not a panel screen. **Decision D5.**[m
 [m
 ---[m
 [m
 ## 5. Part B — Token unification[m
 [m
[31m-Phase 1 adds the inert, collision-free identity tokens (`--surface-2`/`--color-surface-2`, `--grad-brand`, `--radius`). Phase 2 wires the radius scale into Tailwind and repoints components; Phase 4 adds `--font-mono` with its consumers.[m
[32m+[m[32mAdd to `globals.css` (`:root` + `@theme inline`) so the dashboard gains the site's missing tokens. None of these change existing colors; they add the _missing_ primitives.[m
[32m+[m
[32m+[m[32m```css[m
[32m+[m[32m:root {[m
[32m+[m[32m  --surface-2: #f5f5fa; /* light: nested panels */[m
[32m+[m[32m  --radius: 14px;[m
[32m+[m[32m  --radius-sm: 9px;[m
[32m+[m[32m  --grad-brand: linear-gradient(135deg, var(--color-primary), var(--color-accent-light));[m
[32m+[m[32m  --font-mono: ui-monospace, "JetBrains Mono", "Fira Code", "SF Mono", monospace;[m
[32m+[m[32m}[m
[32m+[m[32m.dark {[m
[32m+[m[32m  --surface-2: #1c2230;[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m@theme inline {[m
[32m+[m[32m  --color-surface-2: var(--surface-2); /* enables bg-surface-2 */[m
[32m+[m[32m  --radius-lg: var(--radius); /* enables rounded-lg = 14px */[m
[32m+[m[32m  --radius-md: var(--radius-sm); /* enables rounded-md = 9px */[m
[32m+[m[32m  --font-mono: var(--font-mono); /* enables font-mono */[m
[32m+[m[32m}[m
[32m+[m[32m```[m
 [m
[31m-| Token                      | Why                                                             | Phase                          |[m
[31m-| -------------------------- | --------------------------------------------------------------- | ------------------------------ |[m
[31m-| `--radius` / `--radius-sm` | One radius scale (14/9) instead of 6/8/12 ad-hoc                | 1 (value) / 2 (wire + repoint) |[m
[31m-| `--grad-brand`             | Brand gradient for primary CTAs (red→violet), matching the site | 1 (token) / 2 (Button)         |[m
[31m-| `--surface-2`              | Nested panels / table headers / inset rows                      | 1                              |[m
[31m-| `--font-mono`              | Code blocks, terminal, IDs, endpoints                           | 4                              |[m
[31m-| `--text-muted` reconcile   | Pick one value site↔panel (`#a1a1aa` recommended)               | 2                              |[m
[32m+[m[32m| Token                      | Why                                                             | Consumers                                                   |[m
[32m+[m[32m| -------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------- |[m
[32m+[m[32m| `--radius` / `--radius-sm` | One radius scale (14/9) instead of 6/8/12 ad-hoc                | Button, Card, Modal, Input, Select                          |[m
[32m+[m[32m| `--grad-brand`             | Brand gradient for primary CTAs (red→violet), matching the site | Button `primary`, hero/CTA surfaces                         |[m
[32m+[m[32m| `--surface-2`              | Nested panels / table headers / inset rows                      | Card.Section, DataTable header, inputs                      |[m
[32m+[m[32m| `--font-mono`              | Code blocks, terminal, IDs, endpoints                           | ConsoleLogViewer, code snippets, `localhost:20128/v1` chips |[m
[32m+[m[32m| `--text-muted` reconcile   | Pick one value site↔panel                                       | global                                                      |[m
 [m
[31m-**D2 (text-muted):** site `#8b8b9e` vs dashboard `#a1a1aa`. Recommend keeping the **dashboard's `#a1a1aa`** and updating the _site_ to match. Cosmetic.[m
[32m+[m[32m**Decision D2 (text-muted):** site `#8b8b9e` vs dashboard `#a1a1aa`. Recommend keeping the **dashboard's `#a1a1aa`** (it's the live product, slightly higher contrast) and updating the _site_ to match. Low priority, cosmetic.[m
 [m
 ---[m
 [m
[31m-## 6. Part C — Component standardization (Phases 2–4)[m
[32m+[m[32m## 6. Part C — Component standardization[m
 [m
[31m-Custom components (no shadcn/Radix), Tailwind v4, semantic tokens **mostly** adopted (195 files import the shared barrel). The work is removing the **bypasses**. Home: `src/shared/components/`.[m
[32m+[m[32mThe component layer is **custom** (no shadcn/Radix), Tailwind v4, semantic tokens **mostly** adopted (`bg-surface`, `border-white/10`, `ring-primary`) — good adoption (195 files import the shared barrel). The work is removing the **bypasses**. Home: `src/shared/components/`.[m
 [m
[31m-| #   | Item                                   | File(s)                                                                                                                  | Problem → Target                                                                                                    | Phase |[m
[31m-| --- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ----- |[m
[31m-| C1  | **Radius alignment**                   | `Button.tsx:14-18`, `Card.tsx:39`, `Modal.tsx`, `Input.tsx`, `Select.tsx`                                                | mixed 6/8/12px → `--radius`/`--radius-sm` (14/9)                                                                    | 2     |[m
[31m-| C2  | **Button gradient + `accent` variant** | `Button.tsx:5-12`                                                                                                        | primary is flat red→red; align to `--grad-brand`; add missing `accent` variant. ~195 importers — highest visibility | 2     |[m
[31m-| C3  | **Tables**                             | `DataTable.tsx:122-176`, `logTableStyles.ts`, `globals.css:405-414`                                                      | 100% inline hardcoded rgba + non-existent vars; migrate to tokens, retire divergent styles                          | 3     |[m
[31m-| C4  | **Centralize status colors**           | `flow/edgeStyles.ts`, `TokenHealthBadge.tsx`, `DegradationBadge.tsx`, `ProviderCascadeNode.tsx`, `Badge.tsx` + 5 helpers | 6+ copies of the same hex → one module off `--color-success/warning/error`                                          | 3     |[m
[31m-| C5  | **Card border**                        | `Card.tsx:39`                                                                                                            | `border-white/5` → brand `/8`                                                                                       | 2     |[m
[31m-| C6  | **Focus ring reconcile** ✅ DONE       | `globals.css` `--focus-ring` (accent) vs form controls' `ring-primary/30`                                                | unified on **accent (violet)** to match the global ring + disambiguate from the red error ring; error stays red     | 4     |[m
[31m-| C7  | **Add `Checkbox` + `Textarea`**        | raw `<input>`/`<textarea>` w/ inline `accentColor:#6366f1`                                                               | token-driven primitives                                                                                             | 4     |[m
[31m-| C8  | **Hardcoded-hex sweep**                | `ConsoleLogViewer.tsx:240`, `ComboLiveStudio.tsx:306`, Modal dots, ~14 chart files                                       | literals → tokens                                                                                                   | 4     |[m
[31m-| C9  | **`cn()` → clsx + tailwind-merge**     | `src/shared/utils/cn.ts`                                                                                                 | conflicting classes stack; needed for C1 overrides                                                                  | 2     |[m
[32m+[m[32mRanked by impact × reach:[m
[32m+[m
[32m+[m[32m| #   | Item                                       | File(s)                                                                                                                                                                                       | Problem → Target                                                                                                                                                                                                                                 |[m
[32m+[m[32m| --- | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |[m
[32m+[m[32m| C1  | **Radius alignment**                       | `Button.tsx:14-18`, `Card.tsx:39`, `Modal.tsx`, `Input.tsx`, `Select.tsx`                                                                                                                     | mixed 6/8/12px → repoint to `--radius`/`--radius-sm` (14/9)                                                                                                                                                                                      |[m
[32m+[m[32m| C2  | **Button gradient + `accent` variant**     | `Button.tsx:5-12`                                                                                                                                                                             | primary is flat red→red (`from-primary to-primary-hover`); align to `--grad-brand` (red→violet) and add the missing `accent` variant (indigo `#6366f1` is unused by buttons) — **highest visibility, ~195 importers**. **Decision D1.**          |[m
[32m+[m[32m| C3  | **Tables**                                 | `DataTable.tsx:122-176`, `logTableStyles.ts`, `globals.css:405-414` (Ant remnants)                                                                                                            | `DataTable` is 100% inline hardcoded rgba + references non-existent vars (`--text-secondary`, `--bg-table-header`); migrate to tokens, retire the 2 divergent table styles. Tables are everywhere (providers/connections/logs) — worst offender. |[m
[32m+[m[32m| C4  | **Centralize status colors**               | `flow/edgeStyles.ts:7-12`, `TokenHealthBadge.tsx:14-19`, `DegradationBadge.tsx`, `ProviderCascadeNode.tsx`, `Badge.tsx`, +5 `statusColor` helpers                                             | 6+ copies of the same `#22c55e/#f59e0b/#ef4444` hex; create one `statusColors` module driven off `--color-success/warning/error`. Critical for circuit-breaker / cooldown / lockout badges to read consistently.                                 |[m
[32m+[m[32m| C5  | **Card border**                            | `Card.tsx:39`                                                                                                                                                                                 | uses `border-white/5`; brand border is `/8` → align                                                                                                                                                                                              |[m
[32m+[m[32m| C6  | **Focus ring reconcile**                   | `globals.css:183` vs component `ring-primary/30`                                                                                                                                              | global `:focus-visible` is indigo (`--color-accent`), components are red (`ring-primary`) — pick one (recommend **accent/indigo** globally, it reads as the "interactive" color)                                                                 |[m
[32m+[m[32m| C7  | **Add `Checkbox` + `Textarea` primitives** | currently raw `<input>`/`<textarea>` with inline `accentColor:#6366f1` (e.g. `ColumnToggle.tsx:91`)                                                                                           | create token-driven primitives                                                                                                                                                                                                                   |[m
[32m+[m[32m| C8  | **Hardcoded-hex sweep**                    | `ConsoleLogViewer.tsx:240` (`#161b22`/`#30363d`), `ComboLiveStudio.tsx:306` (`#6366f120`), Modal traffic dots `Modal.tsx:149-159`, ~14 chart/component files with literal `#6366f1`/`#a855f7` | replace literals with `bg-surface`/`border-border`/`text-accent` etc.                                                                                                                                                                            |[m
[32m+[m[32m| C9  | **`cn()` → clsx + tailwind-merge**         | `src/shared/utils/cn.ts`                                                                                                                                                                      | current `cn` just joins; conflicting classes stack (a `className="rounded-2xl"` override won't replace a primitive's `rounded-lg`). Needed for C1 overrides to behave.                                                                           |[m
 [m
 **Already on-brand (token-driven, only need radius):** `Badge`, `Toggle`, `SegmentedControl`, `Input`, `Select`.[m
 [m
 ---[m
 [m
[31m-## 7. Rollout plan[m
[32m+[m[32m## 7. Rollout plan (phased, each phase shippable + testable)[m
[32m+[m
[32m+[m[32m- **Phase 1 — Grid + tokens (low risk, high visibility).**[m
[32m+[m[32m  1. Add grid + identity tokens to `globals.css` (Part A §4.3, Part B §5).[m
[32m+[m[32m  2. Add `body::before` grid.[m
[32m+[m[32m  3. Remove `bg-bg` from `DashboardLayout.tsx:62`.[m
[32m+[m[32m  4. Verify across themes + key screens (dashboard, providers, logs, login, an error page). Confirm contrast unchanged.[m
[32m+[m[32m     → _Delivers the headline ask. Reversible in one commit._[m
[32m+[m
[32m+[m[32m- **Phase 2 — Primitives radius + Button (C1, C2, C5, C9).** The visible "feel" pass. `cn()` upgrade first so overrides behave.[m
[32m+[m
[32m+[m[32m- **Phase 3 — Tables + status colors (C3, C4).** The largest consistency win; touch the data-heavy screens.[m
 [m
[31m-- **Phase 1 — Grid + identity tokens (THIS PR).** `globals.css` grid + `--surface-2`/`--grad-brand`/`--radius` tokens; `body::before` wallpaper; remove the `bg-bg` blocker; static guard test. Low risk, reversible in one commit.[m
[31m-- **Phase 2 — Primitives (C1, C2, C5) — DONE in this PR.** Semantic radius utilities `rounded-card` (14px) / `rounded-control` (9px) added via `@theme` (custom names, so the default `rounded-sm/md/lg/xl` stay untouched — no 400-file blast); Card/Modal → 14px, Button/Input/Select → 9px; Button primary → `--grad-brand` (red→violet) + new `accent` variant; Card borders → the `border-border` token (0.08). **Deferred:** `cn()`→tailwind-merge (C9) needs new deps; the ad-hoc `rounded-lg` sweep (326 files) is left as-is since the primitives carry the bulk of the surface.[m
[31m-- **Phase 3 — Status colors + tables (C3, C4) — DONE in this PR.** ✅ **C4** (`src/shared/constants/statusColors.ts` — `STATUS_HEX` single source; `flow/edgeStyles.ts` + `TokenHealthBadge` repointed, faithful/same hex). ✅ **`--font-mono`** token. ✅ **C3 (DataTable)** — replaced every inline rgba + the dead `var(--bg-table-header)` / `var(--text-secondary)` fallbacks with a `--table-*` token set (`--table-header-bg/-row-zebra/-row-hover/-cell-border/-row-selected`) whose **dark values exactly equal the old hardcoded rgba** (dark byte-identical) and whose light values fix the previously-always-dark light theme. Header border → `--color-border`, secondary text → `--color-text-muted`. **Wants a visual pass before merge.** (Not touched: `logTableStyles.ts` and the legacy Ant `.ant-table` rules — separate, lower priority.)[m
[31m-- **Phase 4 — Cleanup (C6, C7, C9 done; C8 pending).** ✅ **C9** `cn()` → `twMerge(clsx(...))` (clsx + tailwind-merge added as deps) — a caller's `className` now correctly _replaces_ a primitive's conflicting class instead of stacking. ✅ **C7** new `Checkbox` + `Textarea` primitives (token-driven, exported from the barrel; additive — adoption of the 32 raw checkboxes / 41 raw textareas can follow incrementally). ✅ **C6** focus-ring reconcile — the form controls (`Input`/`Select`/`Textarea`/`Toggle`/`Checkbox`) now focus on the **accent (violet)** ring to match the global `--focus-ring` and to stop colliding with the red error ring; the red error state is unchanged. ⏳ **C8 hex-sweep is NOT a blind find/replace** — confirmed offenders that are _intentional_ and must stay: `ConsoleLogViewer.tsx:240` (always-dark terminal), `TokenHealthBadge` popover, ReactFlow SVG strokes. Only migrate hex genuinely meant to be theme-aware.[m
[32m+[m[32m- **Phase 4 — Cleanup (C6, C7, C8).** Focus ring, new primitives, hardcoded-hex sweep.[m
 [m
[31m-Each phase: `npm run lint` + `npm run typecheck:core` + a visual pass.[m
[32m+[m[32mEach phase: `npm run lint` + `npm run typecheck:core` + a visual pass. Per repo rule, production-code changes ship with tests where applicable (token/CSS changes are visual — validated by screenshots; component API changes get unit coverage).[m
 [m
 ---[m
 [m
[31m-## 8. Open decisions (recommendations)[m
[32m+[m[32m## 8. Open decisions (need your call before/while implementing)[m
 [m
[31m-- **D1 — Button primary:** keep red→red or switch to **red→violet `--grad-brand`**? Rec: **red→violet** (Phase 2).[m
[31m-- **D2 — Grid line color:** **neutral** (site style) — chosen — vs brand-red. Size **32px** (shrunk ~30% from the original 46px on owner feedback — 46px cells read too large on the dashboard layout).[m
[31m-- **D3 — Chrome vibrancy:** sidebar/header **solid** — chosen.[m
[31m-- **D4 — Auth/login grid:** ✅ **DONE (Phase 5)** — opaque `bg-bg` removed from every standalone full-screen wrapper (not just login), so the grid shows on all screens. See §4.6.[m
[31m-- **D5 — Landing page:** leave animated splash as-is. Chosen.[m
[31m-- **D6 — Radius 14/9 product-wide:** Rec: yes (Phase 2).[m
[31m-- **D7 — Phase 1 ships first:** Chosen.[m
[31m-- **D8 — Layout width (Phase 5):** the dashboard content shell was capped at `max-w-7xl` (1280px), centering with wide empty side gutters on large monitors. ✅ **DONE** — raised to a fluid `max-w-[3840px]` (true 4K): content now follows the viewport up to ~4K and only centers beyond it (`DashboardLayout.tsx`). Deliberately-narrow pages stay narrow by design (`ProviderOnboardingWizard` max-w-5xl, `Rtk`/`CavemanContextPageClient` max-w-6xl).[m
[31m-- **D9 — Opaque data tables (Phase 6):** with the dashboard content area now transparent (so the grid wallpaper shows through, Phase 5), data tables whose container was _not_ an opaque surface let the grid bleed through their transparent even-rows / low-alpha zebra. ✅ **DONE** — every card-less table now paints `bg-surface` (or, for the `<DataTable>` primitive, `background: var(--color-surface)` on its scroll container). Fixed: `DataTable` (primitive), `ProxyLogger`/`RequestLoggerV2` (their `<Card>` `bg-black/5 dark:bg-black/20` tint was winning over the Card's `bg-surface` via tailwind-merge → ~95% transparent), `BatchListTab`/`FilesListTab`/`CacheEntriesTab`/`ReasoningCacheTab`/`cache page`/`FreePoolTab`/`ModelMappingTable`/`HeaderTable`, plus the two CSS-grid "tables" in the cache views (`bg-surface/35` → `bg-surface`). Tables already inside a `<Card>`/Modal were verified opaque and deliberately left untouched (bg-surface there is a redundant no-op). The grid itself needed **no change** — dashboard `body::before` is byte-identical to the site (`--grid-size: 32px`); any "bigger grid" seen on a running instance is a stale pre-`#4143` build, not code. Guarded by `tests/unit/design-grid-background.test.ts` (Phase 6 block).[m
[32m+[m[32m- **D1 — Button primary look.** Keep the current **red→red** gradient, or switch the product's primary buttons to the **red→violet `--grad-brand`** (matches the site CTAs)? _(Affects every primary button.)_ Recommend: **red→violet**, with `--grad-brand`.[m
[32m+[m[32m- **D2 — Grid line color.** **Neutral** lines (site style: faint white/black, `rgba(255,255,255,0.035)`) — calm, content-first — **or** the landing's **brand-red** lines? Recommend: **neutral** (matches the site's interior pages; red is louder and can tint readability). Size **46px** (site) to retire the landing's 50px drift.[m
[32m+[m[32m- **D3 — Chrome vibrancy.** Sidebar/header stay **solid** (grid in content area only), or go **translucent** so the grid runs behind them? Recommend: **solid** (calmer; less risk).[m
[32m+[m[32m- **D4 — Auth/login grid.** Soften `login/page.tsx` wrappers so the grid shows there too? Recommend: **yes** (cheap, more cohesive).[m
[32m+[m[32m- **D5 — Landing page.** Leave its animated splash bg as-is, or align it to the global grid? Recommend: **leave as-is**.[m
[32m+[m[32m- **D6 — Radius value.** Adopt **14/9** everywhere (bigger, softer, site-matching) — confirm you want this product-wide shift. Recommend: **yes**, it's the single biggest "one identity" signal.[m
[32m+[m[32m- **D7 — Scope of first PR.** Ship **Phase 1 only** first (grid + tokens), then iterate? Recommend: **yes** — validate the wallpaper live before the component waves.[m
 [m
 ---[m
 [m
 ## 9. Out of scope / risks[m
 [m
 - **No palette change** — colors already match; we only add missing tokens. Zero risk of recoloring the product.[m
[31m-- **No theme-engine change** — keep `.dark` + Zustand store.[m
[31m-- **Radius shift (Phase 2) is broad** — touches every card/button/input; eyeball busy screens (tables, modals) before merge.[m
[31m-- **Tables (C3)** carry the most hardcoded styling and the highest regression surface — isolate in their own PR.[m
[32m+[m[32m- **No theme-engine change** — keep `.dark` + Zustand store; don't migrate to `next-themes` or to the site's `data-theme`.[m
[32m+[m[32m- **Radius shift is broad** (D6) — it touches every card/button/input; that's the point, but it's the one change worth eyeballing on busy screens (tables, modals) before merge.[m
[32m+[m[32m- **Tables (C3)** carry the most hardcoded styling and the highest regression surface — isolate in its own PR with before/after screenshots.[m
[32m+[m[32m- **Worktree isolation (repo hard-rule #19):** implementation runs in a dedicated worktree on a branch cut from the confirmed base (likely `release/v3.8.28`), never on the shared checkout. This doc is the only artifact written to the working tree so far.[m
 [m
 ---[m
 [m
 ## 10. Reference index[m
 [m
[31m-| Area                              | Path                                                                                                                 |[m
[31m-| --------------------------------- | -------------------------------------------------------------------------------------------------------------------- |[m
[31m-| Dashboard tokens                  | `src/app/globals.css` (`:root`, `.dark`, `@theme inline`, `body`, `body::before`)                                    |[m
[31m-| Theme store                       | `src/store/themeStore.ts`, `src/shared/components/ThemeProvider.tsx`, `src/shared/constants/appConfig.ts:9-11`       |[m
[31m-| Panel shell (grid unblocked here) | `src/shared/components/layouts/DashboardLayout.tsx`                                                                  |[m
[31m-| Chrome                            | `src/shared/components/Header.tsx:207`, `src/shared/components/Sidebar.tsx:430`                                      |[m
[31m-| Grid precedent                    | `src/app/landing/page.tsx:16-26`                                                                                     |[m
[31m-| Primitives                        | `src/shared/components/{Button,Card,Input,Select,Badge,Modal,Toggle,SegmentedControl,Loading,Tooltip,DataTable}.tsx` |[m
[31m-| Status-color sources              | `flow/edgeStyles.ts`, `TokenHealthBadge.tsx`, `DegradationBadge.tsx`, `logTableStyles.ts`                            |[m
[31m-| `cn` util                         | `src/shared/utils/cn.ts`                                                                                             |[m
[31m-| Phase 1 guard test                | `tests/unit/design-grid-background.test.ts`                                                                          |[m
[31m-| Site reference                    | `_mono_repo/omnirouteSite/css/tokens.css`, `css/base.css`                                                            |[m
[32m+[m[32m| Area                       | Path                                                                                                                                     |[m
[32m+[m[32m| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |[m
[32m+[m[32m| Dashboard tokens           | `src/app/globals.css:30-179` (`:root`, `.dark`, `@theme inline`), `body` `:206`                                                          |[m
[32m+[m[32m| Theme store                | `src/store/themeStore.ts`, `src/shared/components/ThemeProvider.tsx`, `src/shared/constants/appConfig.ts:9-11`                           |[m
[32m+[m[32m| Panel shell (grid blocker) | `src/shared/components/layouts/DashboardLayout.tsx:62`                                                                                   |[m
[32m+[m[32m| Chrome                     | `src/shared/components/Header.tsx:207`, `src/shared/components/Sidebar.tsx:430`                                                          |[m
[32m+[m[32m| Grid precedent             | `src/app/landing/page.tsx:16-26`                                                                                                         |[m
[32m+[m[32m| Primitives                 | `src/shared/components/{Button,Card,Input,Select,Badge,Modal,Toggle,SegmentedControl,Loading,Tooltip,DataTable}.tsx`, barrel `index.tsx` |[m
[32m+[m[32m| Status-color sources       | `flow/edgeStyles.ts`, `TokenHealthBadge.tsx`, `DegradationBadge.tsx`, `logTableStyles.ts`                                                |[m
[32m+[m[32m| `cn` util                  | `src/shared/utils/cn.ts`                                                                                                                 |[m
[32m+[m[32m| Site reference             | `_mono_repo/omnirouteSite/css/tokens.css`, `css/base.css` (grid `body::before`)                                                          |[m
