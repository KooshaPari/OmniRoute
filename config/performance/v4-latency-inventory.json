{
  "schemaVersion": 1,
  "owner": "apps/bff",
  "registeredRouteCount": 68,
  "registeredRouteSha256": "ef29b19de19768f639e063595ab2560c95dc1af7951eec650890edf8aed98261",
  "producers": [
    {
      "path": "/api/v1/telemetry/web-vitals",
      "method": "POST",
      "state": "validated-log-only",
      "consumer": "none"
    }
  ],
  "consumers": [
    {
      "pathPrefix": "/api/dashboard/observability/",
      "state": "explicitly-unavailable",
      "reason": "No runtime aggregation store is implemented."
    },
    {
      "pathPrefix": "/api/dashboard/performance",
      "state": "explicitly-unavailable",
      "reason": "No model latency aggregation source is implemented."
    },
    {
      "pathPrefix": "/api/dashboard/cache",
      "state": "explicitly-unavailable",
      "reason": "No cache metrics source is implemented."
    },
    {
      "pathPrefix": "/api/dashboard/compression/stats",
      "state": "explicitly-unavailable",
      "reason": "No compression metrics source is implemented."
    },
    {
      "pathPrefix": "/api/dashboard/diagnostics/full",
      "state": "explicitly-unavailable",
      "reason": "No diagnostics metrics source is implemented."
    },
    {
      "pathPrefix": "/api/dashboard/keys/:id/usage",
      "state": "explicitly-unavailable",
      "reason": "No per-key usage aggregation source is implemented."
    },
    {
      "pathPrefix": "/api/dashboard/keys",
      "state": "explicitly-unavailable",
      "reason": "No persistent key store is implemented; no key material is fabricated."
    },
    {
      "pathPrefix": "/api/dashboard/sessions",
      "state": "explicitly-unavailable",
      "reason": "No session store is implemented; no session activity is fabricated."
    }
  ],
  "benchmarkNetworkPolicy": {
    "mode": "deny-non-loopback-network",
    "coverage": ["global.fetch", "Bun.connect", "node:net.connect", "node:net.createConnection", "node:dns.lookup", "node:dns.resolve"]
  },
  "benchmarkedRoutes": [
    { "path": "/healthz", "method": "GET", "budgetName": "/healthz" },
    {
      "path": "/api/dashboard/health",
      "method": "GET",
      "budgetName": "/api/dashboard/health"
    }
  ],
  "legacyBudgetScope": "Entries outside benchmarkedRoutes describe the legacy service and are not evidence for v4."
}
