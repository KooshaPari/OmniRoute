[package]
name = "pheno-port-adapter"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
<<<<<<< HEAD
description = "Hexagonal L4 Port/Adapter pattern — reference implementation of the Port trait and Adapter impl contract (ADR-038)"
license = "MIT"
repository = "https://github.com/KooshaPari/phenotype-apps"
documentation = "https://github.com/KooshaPari/phenotype-apps/tree/main/pheno-port-adapter"
keywords = ["port", "adapter", "hexagonal", "phenotype", "l4"]
categories = ["development-tools", "embedded"]
authors = ["Koosha Pari <koosha@phenotype.local>"]

[workspace]

[dependencies]
thiserror = "2.0"
# Canonical OTLP wire-format export substrate (ADR-037).
# `pheno-port-adapter` uses `pheno-otel` to surface Port/Adapter
# resolution traces and lifecycle spans to OTLP/HTTP collectors when
# wired into a wider observability stack (per ADR-023 Rule 3.1:
# every substrate ships observability).
pheno-otel = { path = "../pheno-otel" }
