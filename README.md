# omniroute-wtrees

**Category**: Scratch / tooling archive
**Source path**: `/Users/kooshapari/CodeProjects/Phenotype/repos/omniroute-wtrees/`
**Gitified**: 2026-07-15
**Airlock**: auto-init from gitification pass

## Contents

container for omniroute worktree checkouts (1.7G, 14 .git files); only scaffolding tracked

## Provenance

This repository was created on 2026-07-15 by an automated gitification pass over the
`~/CodeProjects/Phenotype/repos/` container. The original directory was a "plain
dir" (no `.git/` directory at its root) inside a multi-repo container. Per
container policy, every top-level entry must be either (a) a tracked git repo,
(b) archived, or (c) an explicitly-marked drop/temp dir.

This repo is the (a)-tracked form of `omniroute-wtrees/`.

## Notes

- See `.gitignore` for what is excluded from the initial commit.
- If this dir is intended to fold into another repo as a sub-crate or sub-module,
  the merge should be performed in that other repo and this scratch repo can be
  deleted from `KooshaPari/` afterwards.
