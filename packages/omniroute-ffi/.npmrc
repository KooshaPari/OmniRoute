# Optional dependency hint for pnpm/npm.
# Marks the per-platform @omniroute/ffi-* packages as truly optional — install
# does not abort if they fail to download on an unsupported host.
optional=true
package-lock=false