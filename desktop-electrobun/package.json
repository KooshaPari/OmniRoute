{
  "name": "@phenotype/desktop-shell",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun run prepare:web && electrobun build",
    "build:release": "bun run prepare:web && electrobun build --release",
    "dev": "electrobun dev",
    "prepare:web": "node ../scripts/build/prepare-electrobun-web.mjs",
    "smoke:gateway": "node ../scripts/build/smoke-electrobun-gateway.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/three": "^0.181.0",
    "typescript": "^5.7.0"
  }
}
